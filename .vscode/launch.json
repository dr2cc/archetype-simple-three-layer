{
    // Используйте IntelliSense, чтобы узнать о возможных атрибутах.
    // Наведите указатель мыши, чтобы просмотреть описания существующих атрибутов.
    // Для получения дополнительной информации посетите: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Env +",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/app",
            "env": {
                // "DATABASE_DSN":"postgres://postgres:qwerty@localhost:5434/postgres?sslmode=disable",
                "CONFIG_PATH":"${workspaceFolder}/config/config.yml"
            }
        },
        {
            "name": "Only debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/app", // указывает, что запускать (пакет main)
            "cwd": "${workspaceFolder}" // (current working directory) устанавливает рабочий каталог в корень проекта
        }
    ]
}